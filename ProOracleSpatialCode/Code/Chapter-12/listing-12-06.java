// Listing 12-6. Zoom Actions

// -----------------------------------------------------------------------
// [Zoom XXX] button clicked
// Zoom in or out by a fixed factor
// -----------------------------------------------------------------------
else if (userAction.equals("Zoom In"))
  mv.zoomIn(zoomFactor);
else if (userAction.equals("Zoom Out"))
  mv.zoomOut(zoomFactor);
